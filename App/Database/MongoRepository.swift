//
//  MongoRepository.swift
//  VaporApp
//
//  Created by Sebastien Arbogast on 22/05/2016.
//
//

import Foundation
//import MongoKitten
import Vapor
/*
enum MongoRepositoryError:ErrorProtocol {
    case CouldNotConnect
}

class MongoRepository {
    let username = "vaportodolistbackend"
    let password = "TtfJERaHjWLyX49P"
    let host = "ds011903.mlab.com"
    let port = "11903"
    let db = "vaportodolistbackend"
    
    static let sharedRepository = try? MongoRepository()
    
    private let server:MongoKitten.Server
    var database:MongoKitten.Database {
        return server[db]
    }
    
    init() throws {
        try server = Server("mongodb://\(username):\(password)@\(host):\(port)", automatically: true)
        Log.info("Database connection created")
    }
}
 */
